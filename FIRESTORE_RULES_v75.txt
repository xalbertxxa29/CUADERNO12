// Firestore Rules - ACTUALIZADO v75
// Reemplaza COMPLETAMENTE el contenido de tus reglas de Firestore con esto

rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Regla general: Cualquier usuario autenticado puede leer y escribir en su propio documento en 'usuarios'
    match /USUARIOS/{userId} {
      allow create: if request.auth != null;
      allow read: if request.auth != null && request.auth.uid == resource.data.usuarioID;
      allow update: if request.auth != null && request.auth.uid == resource.data.usuarioID;
      allow delete: if false; // No permitir eliminación
    }

    // 🆕 NUEVA REGLA: Control de Tiempos - Todos los usuarios autenticados pueden crear y actualizar registros de control
    match /CONTROL_TIEMPOS_USUARIOS/{document=**} {
      // Cualquier usuario autenticado puede crear registros de control de tiempo
      allow create: if request.auth != null;
      
      // Cualquier usuario autenticado puede leer registros de control
      allow read: if request.auth != null;
      
      // Cualquier usuario autenticado puede actualizar registros (para finalizar control)
      allow update: if request.auth != null;
      
      // No permitir eliminación (auditoría)
      allow delete: if false;
    }

    // Regla para CUADERNO (registros)
    match /CUADERNO/{document=**} {
      allow create: if request.auth != null;
      allow read: if request.auth != null;
      allow update: if request.auth != null;
      allow delete: if false;
    }

    // Regla para CLIENTE_UNIDAD (catálogos)
    match /CLIENTE_UNIDAD/{document=**} {
      allow read: if request.auth != null;
      allow create: if request.auth != null;
      allow update: if request.auth != null;
      allow delete: if false;
    }

    // Regla por defecto: Denegar todo lo no especificado
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
